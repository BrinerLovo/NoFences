# Extractable components

There are no reusable web/layout components to extract into Petite-Vue. The native WinForms theme helper and menu renderer are code-level primitives, not canvas layout components. Drafts should represent native desktop settings windows using inline controls and the tokens in `theme.md`.
